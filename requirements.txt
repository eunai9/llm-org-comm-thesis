# Runtime dependencies. Pinned loosely now; freeze exact versions before the
# main experimental runs (October) so results stay reproducible.

# --- config & environment ---
pydantic>=2.7
pydantic-settings>=2.3
PyYAML>=6.0
python-dotenv>=1.0

# --- data processing (Phase 2) ---
pandas>=2.2
pyarrow>=16.0
duckdb>=1.0
numpy>=1.26

# --- LLM providers (Phase 3) ---
anthropic>=0.40
openai>=1.40

# Added in later phases (kept here as a roadmap, install when needed):
spacy==3.8.15
networkx==3.6.1
# en_core_web_sm model: `python -m spacy download en_core_web_sm`
#
# convokit was evaluated for Layer A politeness strategies and dropped:
# its dependency resolution stalled/hung on this environment (heavy,
# sometimes-conflicting tree: nltk corpus downloads, sklearn, a pinned
# spaCy version). Layer A uses a hand-curated lexicon instead -- see
# src/thesis/data/features.py for the citation basis and the scope-cut
# note. Not attempted again without a specific reason to revisit it.
#   rapidfuzz                   -- alias clustering
#   sentence-transformers       -- embeddings, diversity, propagation
#   scikit-learn                -- propagation classifier, real-vs-generated AUC
#   statsmodels                 -- mixed models (Q1)
#   pingouin, krippendorff      -- ICC and inter-rater reliability (Q3)
#   matplotlib                  -- figures
#   jinja2                      -- prompt templates
